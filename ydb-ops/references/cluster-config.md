# Cluster Configuration Reference

TODO: Add cluster configuration reference covering:
- Server-side YAML config structure
- Logging levels and tuning (default_level, sampling, per-component)
- Query cache size tuning
- gRPC settings (keep-alive, message size limits)
- Storage group overcommit settings
- Hive configuration (storage_overcommit, balance_strategy)
- Actor system tuning
- Tablet configuration
- TTL run interval tuning
- BlobStorage configuration
