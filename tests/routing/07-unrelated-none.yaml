description: Routing · Unrelated PostgreSQL question → none

vars:
  user_prompt: |
    How do I write a PostgreSQL trigger that fires on UPDATE and
    logs the changed row to a separate audit table?

assert:
  - type: regex
    value: \bnone\b
  - type: not-regex
    value: \bydb-(core|table)\b
